[project]
name = "graph-prerelease-reqs-additional-deps"
version = "0.1.0"
description = "Test for prerelease stuff"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "langgraph==1.1.5"
]

[[tool.uv.index]]
name = "approved-registry"
url = "https://approved-registry.example.com/simple/"
explicit = true

[tool.uv.sources]
langgraph = { index = "approved-registry" }